my_pipeline:
  target: dev
  outputs:
    dev:
      type: postgres
      threads: 4
      host: postgres      
      port: 5432
      user: "{{ env_var('DB_USER') }}"
      password: "{{ env_var('DB_PASS') }}"
      dbname: "{{ env_var('DB_NAME') }}"
      schema: staging
